# ProPost Assistant Backend

## Run locally

1. Copy `.env.example` to `.env` and fill values.
2. Install dependencies:
   - `npm install`
3. Start dev server:
   - `npm run dev`

Backend runs on `http://localhost:3001`.
